Kapsamlı mobil port dokümantasyonu için önce route, güvenlik ve sayfa kaynaklarını tarıyorum.

Sayfa, güvenlik ve servis dosyalarını paralel okuyorum.

# purchase-fe Mobil Port Dokümantasyonu

Kaynak: `/Users/ysfaltn/IdeaProjects/Anabilim/purchase-fe`  
Kapsam: Login, Dashboard, User/Role/Permission/Workflow CRUD, UserGroupWhiteboard + güvenlik katmanı.

---

## Güvenlik katmanı (ortak)

### PrivateRoute (`src/components/PrivateRoute.tsx`)
- **Kontrol:** MSAL aktif hesap (`accounts.length > 0 && instance.getActiveAccount()`) **veya** `authService.isAuthenticated()` (`localStorage.access_token`).
- **Başarısız:** `<Navigate to="/login" replace />`
- **Not:** Capability kontrolü yok; yalnızca oturum.

### CapabilityRoute (`src/components/CapabilityRoute.tsx`)
- Auth yok → `/login`
- `authService.hasCapability(capability)` false → `/dashboard`
- Bu dokümandaki sayfalarda **kullanılmıyor** (yalnızca PrivateRoute).

### AuthContext (`src/contexts/AuthContext.tsx`)
- Geleneksel login: `authService.login` → token/`user_info` localStorage
- Microsoft: MSAL `LOGIN_SUCCESS` → `POST /api/v1/auth/microsoft/verify-token` → token + userInfo → `/dashboard`
- Loading spinner tüm uygulamayı bloklar
- Logout: MSAL `logoutRedirect` + `authService.logout()`

### authService capability / roller
- `SYSTEM_MANAGE`: roller `BILGI_ISLEM_DEPARTMANI` | `SYSTEM_ADMIN`
- Navigation’da Sistem menüsü (Users/Roles/Workflows/Permissions/UserGroups) **yalnızca** `canSystemManage` ile görünür
- Route seviyesinde bu sayfalarda CapabilityRoute **yok** → URL ile doğrudan erişim mümkün (backend yetkisi ayrı)

### Navigation görünürlük
Sistem menüsü (`/workflows`, `/roles`, `/permissions`, `/users`, `/user-groups/whiteboard`): `hasCapability('SYSTEM_MANAGE')`

---

## Login

- **Route:** `/login` (public; `/` → `/login`)
- **Security:** PrivateRoute yok. Public.
- **Sayfa tipi:** public
- **Amaç / özellikler:** E-posta/şifre + Microsoft (MSAL redirect) ile giriş; hata gösterimi; kayıt linki.
- **Gösterilen alanlar / form fieldları:**
  - `email` — type=email, **required**, HTML5 email, autoFocus
  - `password` — type=password, **required**
  - Ek JS validation yok; hata `AuthContext.error`
- **Tablo kolonları:** Yok
- **Butonlar / aksiyonlar:**
  - Giriş Yap (submit) — `loading || microsoftLoading` iken disabled
  - Microsoft ile Giriş Yap — `instance.loginRedirect({ ...loginRequest, prompt: 'create' })`
  - Ücretsiz üye olun → `navigate('/register')` (**App.tsx’te `/register` route yok**)
- **Filtreler / arama:** Yok
- **Modal/popup:** Yok
- **Kullanılan API’ler:**
  - `authService.login` → `POST /api/auth/login` `{ email, password }`
  - MSAL sonrası: `POST /api/v1/auth/microsoft/verify-token` `{ accessToken, microsoftId, email, name }`
- **Navigasyon:** Başarı → `/dashboard` (yeni/eski kullanıcı ayrımı aynı). Microsoft redirect sonrası AuthContext → `/dashboard`
- **Edge cases / conditional UI:**
  - `error` kırmızı alert
  - Çift loading (form / Microsoft)
  - `isNewlyRegistered` localStorage okunur ama her iki dal da `/dashboard`
  - `autoComplete="off"`

---

## Dashboard

- **Route:** `/dashboard`
- **Security:** `PrivateRoute`. CapabilityRoute yok. Sayfa içi: `SATIN_ALMA_DEPARTMANI` | `PURCHASE_MANAGER` → “Onaylananlar” + pending listeden senior-forwarded çıkarılır.
- **Sayfa tipi:** detail / özet (dashboard)
- **Amaç / özellikler:** Karşılama; talep istatistikleri; işlemdeki / onay bekleyen listeler; satın alma için üst onaydan iletilenler; görüldü/görülmedi (localStorage).
- **Gösterilen alanlar / form fieldları:** Form yok. Kartlar: Bu Ay Talepler, Bugün Talepler, Onay Bekleyen, İşlemde. Liste satırları: title, requester ad-soyad, items.length, createdAt, rozetler.
- **Tablo kolonları:** Tablo yok; card listeleri.
- **Butonlar / aksiyonlar:**
  - Detay → `/purchase-requests/:id`
  - Tüm işlemdeki… → `/purchase-requests` (>5 ise)
  - Tüm onay bekleyen… → `/purchase-requests?status=pending` (>5 ise)
- **Filtreler / arama:** Yok (client-side status filtreleri)
- **Modal/popup:** Yok
- **Kullanılan API’ler:**
  - `purchaseRequestService.getMyRequests` → `GET /api/purchase-requests/my-requests`
  - `getPendingApprovals` → `GET /api/purchase-requests/pending-approvals`
  - (satın alma) `getSeniorForwardedPendingApprovals` → `GET /api/purchase-requests/pending-approvals/senior-forwarded`
  - `authService.getUserInfo` / `getEffectiveRoles` / `getCurrentUser` (local)
  - `getSeenPendingApprovalIds` / `pruneSeenPendingApprovals` (localStorage)
- **Navigasyon:** Detay / liste linkleri yukarıda; Navigation bar
- **Edge cases / conditional UI:**
  - `isPurchasingStaff` → “Onaylananlar” (max 8); pending’den forwarded ID’ler çıkarılır
  - Görülmemiş: sol şerit + nokta; görülmüş: gri + “Görüntülendi”
  - Pending: “Kendi talebiniz” vs “Size onay bekliyor”
  - İstatistik `PARTIAL_APPROVAL`, liste `PARTIALLY_APPROVED` — tutarsızlık riski
  - `formatAmount` tanımlı ama kullanılmıyor
  - Loading spinner / error metni

---

## UserList

- **Route:** `/users`
- **Security:** PrivateRoute. CapabilityRoute yok. Menü: SYSTEM_MANAGE. Sayfa içi rol/capability check yok.
- **Sayfa tipi:** list
- **Amaç / özellikler:** Kullanıcı listesi; arama/filtre/sıralama/pagination; sil; create/edit’e git; success flash (`location.state.message`).
- **Gösterilen alanlar / form fieldları:** Form yok (filtre inputları aşağıda).
- **Tablo kolonları:**
  | Kolon | Sıralanabilir | İçerik |
  |-------|---------------|--------|
  | Ad Soyad | firstName | firstName + lastName |
  | E-posta | email | email |
  | Departman | department | veya — |
  | Pozisyon | position | veya — |
  | Roller | roles | badge’ler |
  | Durum | hayır | Aktif/Pasif |
  | İşlemler | hayır | Düzenle, Sil |
- **Butonlar / aksiyonlar:** Yeni Kullanıcı → `/users/create`; Düzenle → `/users/edit/:id`; Sil → `confirm` + delete; Filtreleri temizle; pagination Önceki/Sonraki/sayfa
- **Filtreler / arama:**
  - Arama: email, firstName, lastName, department, position (client)
  - Durum: all | active
  - Departman: unique listeden select
  - Rol: flatMap roles select
  - Sayfa: 10/page; kolon tıklayınca sort asc/desc
- **Modal/popup:** `window.confirm` silme
- **Kullanılan API’ler:**
  - `userService.getAllUsers` → `GET /api/users`
  - `userService.deleteUser` → `DELETE /api/users/{id}`
- **Navigasyon:** create, edit, success state clear
- **Edge cases:** Boş/filtre sonucu boş; aktif sayısı başlıkta; success 5 sn; beklenmeyen format hatası

---

## UserCreate

- **Route:** `/users/create`
- **Security:** PrivateRoute. Menü SYSTEM_MANAGE. Sayfa içi check yok.
- **Sayfa tipi:** create
- **Amaç / özellikler:** Yeni kullanıcı + roller + yönetici Combobox.
- **Gösterilen alanlar / form fieldları:**
  | Alan | Required | Validation / not |
  |------|----------|------------------|
  | email | evet | type=email |
  | firstName | evet | text |
  | lastName | evet | text |
  | phone | evet | type=tel, placeholder +90555… |
  | department | evet | free text |
  | position | evet | free text |
  | Yönetici (manager) | hayır | Combobox; aktif kullanıcılar; ad/email ara |
  | roles | evet (HTML multiple) | aktif roller; Ctrl/Cmd multi-select |
- **Tablo kolonları:** Yok
- **Butonlar / aksiyonlar:** İptal → `/users`; Kaydet → create + `/users` + success message; loading disabled
- **Filtreler / arama:** Manager Combobox query
- **Modal/popup:** Yok (Combobox dropdown)
- **Kullanılan API’ler:**
  - `roleService.getActiveRoles` → `GET /api/roles/active`
  - `userService.getActiveUsers` → `GET /api/users/active`
  - `userService.createUser` → `POST /api/users`
- **Navigasyon:** `/users` (success/cancel)
- **Edge cases:** Roller/yöneticiler yüklenemezse error; Combobox “Sonuç bulunamadı”; şifre alanı yok (backend?)

---

## UserEdit

- **Route:** `/users/edit/:id`
- **Security:** PrivateRoute. Sayfa içi check yok.
- **Sayfa tipi:** edit
- **Amaç / özellikler:** Kullanıcı güncelleme; zimmet formu alanları (okul, lokasyon); aktif zimmetler (`AssignmentManageSection`).
- **Gösterilen alanlar / form fieldları:**
  | Alan | Required | Not |
  |------|----------|-----|
  | email | evet | |
  | firstName | evet | |
  | lastName | evet | |
  | phone | evet | |
  | Yönetici | hayır | Combobox; kendini listeden çıkarır |
  | schoolId | hayır | aktif okullar select; boş = null |
  | Çalışma lokasyonu | hayır | LocationHierarchyPickers (root/middle/leaf); leaf gösterilir |
  | department | evet | “unknown” → boş normalize |
  | position | evet | aynı normalize |
  | roles | evet | multiple |
- Submit payload ekstra: `resolveUserWorkLocationPayload`, `schoolTouched: true`, `workLocationHierarchyTouched: true`
- **Tablo kolonları:** AssignmentManageSection içinde (ürün kolonu açık)
- **Butonlar / aksiyonlar:** İptal → `/users`; Kaydet; AssignmentManageSection refresh/aksiyonları
- **Filtreler / arama:** Manager Combobox
- **Modal/popup:** AssignmentManageSection return vs. modalları (bileşen)
- **Kullanılan API’ler:**
  - `userService.getUserById` → `GET /api/users/{id}`
  - `roleService.getActiveRoles` → `GET /api/roles/active`
  - `userService.getActiveUsers` → `GET /api/users/active`
  - `schoolService.getActiveSchools` → `GET /api/schools/active`
  - `locationService.getAllLocations` → `GET /api/locations`
  - `userService.updateUser` → `PUT /api/users/{id}`
  - `assignmentService.getActiveAssignmentsByUserId` → `GET /api/v1/assignments/user/{userId}/active`
- **Navigasyon:** `/users` success/cancel
- **Edge cases:** İlk yüklemede spinner (`loading && !formData.email`); school/location fail → catch boş; Create’te olmayan okul/lokasyon/zimmet bölümleri

---

## RoleList

- **Route:** `/roles`
- **Security:** PrivateRoute. Menü SYSTEM_MANAGE. Sayfa içi check yok.
- **Sayfa tipi:** list (kart grid, tablo değil)
- **Amaç / özellikler:** Rolleri kartlarda göster; filtre; sil (sistem dışı); create/edit.
- **Gösterilen alanlar:** Kart: displayName, badge (Pasif/Sistem/Aktif), name (mono), tip, durum, izin sayısı, description
- **Tablo kolonları:** Yok (grid)
- **Butonlar / aksiyonlar:** Yeni Rol → `/roles/create`; Düzenle; Sil (yalnızca `!isSystemRole`); İlk Rolü Oluştur (boş); success dismiss
- **Filtreler / arama:**
  - Arama: name, displayName, description
  - Filtre: all | active (`GET /active`) | system (`GET /system`) | custom (client `!isSystemRole`)
  - filter değişince reload (`useEffect` deps: filter)
- **Modal/popup:** `window.confirm` silme
- **Kullanılan API’ler:**
  - `getAllRoles` → `GET /api/roles`
  - `getActiveRoles` → `GET /api/roles/active`
  - `getSystemRoles` → `GET /api/roles/system`
  - `deleteRole` → `DELETE /api/roles/{id}`
- **Navigasyon:** create, edit
- **Edge cases:** Sistem rolünde Sil yok; success 5 sn; custom filtresi client-side all data üzerinde

---

## RoleCreate

- **Route:** `/roles/create`
- **Security:** PrivateRoute. Sayfa içi check yok.
- **Sayfa tipi:** create
- **Amaç / özellikler:** Rol + işlem özeti checkbox’ları + tam permission kataloğu; create sonrası permission’ları tek tek ekle.
- **Gösterilen alanlar / form fieldları:**
  | Alan | Required | Validation |
  |------|----------|------------|
  | name | * | trim; `^[A-Z_]+$`; toUpperCase submit |
  | displayName | * | trim |
  | description | * | textarea |
  | isActive | checkbox | default true |
  | isSystemRole | checkbox | default false |
  | İşlem Yetkileri | — | OPERATION_LABELS: Talep Açma/Düzenleme/Görüntüleme, Onay, Teklif, Sipariş, Kapatma, Sistem, Envanter View/Manage |
  | Tüm permission’lar | — | resource gruplu checkbox; aktif katalog |
- Varsayılan seçim: REQUEST_CREATE + REQUEST_VIEW merge
- **Tablo kolonları:** Yok
- **Butonlar / aksiyonlar:** İptal (dirty confirm); Oluştur (header + form; `loading \|\| permCatalogLoading` disabled)
- **Filtreler / arama:** Yok
- **Modal/popup:** `window.confirm` iptalde
- **Kullanılan API’ler:**
  - `permissionService.getAllPermissions` → `GET /api/permissions`
  - `roleService.createRole` → `POST /api/roles`
  - `getRoleByName` → `GET /api/roles/name/{name}`
  - `addPermissionToRole` → `POST /api/roles/{id}/permissions?permissionName=`
- **Navigasyon:** `/roles` + success
- **Edge cases:** Permission atama fail → warn, yine de navigate; info kartı; katalog loading metni

---

## RoleEdit

- **Route:** `/roles/edit/:id`
- **Security:** PrivateRoute. Sayfa içi check yok.
- **Sayfa tipi:** edit
- **Amaç / özellikler:** Rol güncelle + permission diff (add/remove); orphan permission’lar.
- **Gösterilen alanlar / form fieldları:** Create ile aynı temel alanlar +
  - `isSystemRole` **disabled** eğer `originalData.isSystemRole` (metin uyarısı)
  - Orphan: “Rolde var, katalogda yok”
  - Bilgi: createdAt, updatedAt, mevcut izin badge’leri
- **Tablo kolonları:** Yok
- **Butonlar / aksiyonlar:** İptal (dirty confirm; permission değişimi dirty’de yok); Kaydet; hata sayfasında ← liste
- **Filtreler / arama:** Yok
- **Modal/popup:** confirm iptal
- **Kullanılan API’ler:**
  - `getRoleById` → `GET /api/roles/{id}`
  - `getAllPermissions` → `GET /api/permissions`
  - `updateRole` → `PUT /api/roles/{id}`
  - `addPermissionToRole` / `removePermissionFromRole` → POST/DELETE `.../permissions?permissionName=`
- **Navigasyon:** `/roles`
- **Edge cases:** Load fail → error + geri; saving state; orphan UI

---

## PermissionList

- **Route:** `/permissions`
- **Security:** PrivateRoute. Menü SYSTEM_MANAGE. Sayfa içi check yok. Edit route yok.
- **Sayfa tipi:** list + inline create
- **Amaç / özellikler:** Permission CRUD (create + delete); tek sayfa form+tablo.
- **Gösterilen alanlar / form fieldları (create form):**
  | Alan | Required | Not |
  |------|----------|-----|
  | name | evet | onChange `.toUpperCase()` |
  | displayName | evet | |
  | resource | evet | UPPERCASE |
  | action | evet | UPPERCASE |
  | description | evet | |
  | isActive | state true | UI’da checkbox yok; her zaman true gönderilir |
- **Tablo kolonları:** Name, Görünen Ad, Resource, Action, Açıklama, İşlem (Sil)
- **Butonlar / aksiyonlar:** Ekle (submit); Sil + confirm
- **Filtreler / arama:** Yok
- **Modal/popup:** `window.confirm`
- **Kullanılan API’ler:**
  - `getAllPermissions` → `GET /api/permissions`
  - `createPermission` → `POST /api/permissions`
  - `deletePermission` → `DELETE /api/permissions/{id}`
- **Navigasyon:** Sayfa içi kalır
- **Edge cases:** isActive form’da düzenlenemez; update/edit UI yok; error banner

---

## WorkflowList

- **Route:** `/workflows`
- **Security:** PrivateRoute. Menü SYSTEM_MANAGE. Sayfa içi check yok.
- **Sayfa tipi:** list (kart grid)
- **Amaç / özellikler:** Onay süreçleri listesi; arama/filtre; sil; create/edit.
- **Gösterilen alanlar (kart):** name, Aktif badge, description, category, min/max amount (TRY), steps.length
- **Tablo kolonları:** Yok
- **Butonlar / aksiyonlar:** Yeni Workflow; Düzenle; Sil (confirm); İlk Workflow’u Oluştur; success dismiss
- **Filtreler / arama:**
  - Arama: name, description, category
  - Durum: all | active (`GET /active`) | inactive (client `!isActive` — inactive endpoint kullanılmıyor)
- **Modal/popup:** confirm silme
- **Kullanılan API’ler:**
  - `getAllWorkflows` → `GET /api/approval-workflows`
  - `getActiveWorkflows` → `GET /api/approval-workflows/active`
  - `deleteWorkflow` → `DELETE /api/approval-workflows/{id}`
- **Navigasyon:** `/workflows/create`, `/workflows/edit/:id`
- **Edge cases:** inactive = full list + client filter; success flash

---

## WorkflowCreate

- **Route:** `/workflows/create`
- **Security:** PrivateRoute. Sayfa içi check yok.
- **Sayfa tipi:** create
- **Amaç / özellikler:** Workflow + dinamik onay adımları (sıra, rol API’den, onay tipi).
- **Gösterilen alanlar / form fieldları:**
  | Alan | Required | Validation |
  |------|----------|------------|
  | name | * | trim |
  | category | * | select: IT_EQUIPMENT, OFFICE_SUPPLIES, MARKETING_MATERIALS, TRAINING_SERVICES, CONSULTING_SERVICES, OTHER |
  | description | * | textarea |
  | minAmount | * | number ≥ 0 |
  | maxAmount | * | > minAmount |
  | **Adım:** stepName | * | |
  | **Adım:** roleName | * | `roleService.getActiveRoles` select (displayName göster, name value) |
  | **Adım:** approvalType | — | APPROVE / REJECT / COMMENT |
  | **Adım:** isRequired | checkbox | default true |
  | approverType | gizli | default `ROLE` |
- Adım aksiyonları: Ekle; yukarı/aşağı (disabled uçlarda); Sil (min 1 adım)
- **Tablo kolonları:** Yok
- **Butonlar / aksiyonlar:** İptal (dirty confirm); Oluştur; Adım Ekle
- **Filtreler / arama:** Yok
- **Modal/popup:** confirm iptal
- **Kullanılan API’ler:**
  - `roleService.getActiveRoles` → `GET /api/roles/active`
  - `workflowService.createWorkflow` → `POST /api/approval-workflows`
- **Navigasyon:** `/workflows`
- **Edge cases:** Roller yüklenirken pulse; min 1 adım; steps.length===0 empty state (pratikte nadir)

---

## WorkflowEdit

- **Route:** `/workflows/edit/:id`
- **Security:** PrivateRoute. Sayfa içi check yok.
- **Sayfa tipi:** edit
- **Amaç / özellikler:** Workflow güncelle; Create’e benzer ama farklar önemli.
- **Gösterilen alanlar / form fieldları:** Create alanları + `isActive` checkbox. Adımlar:
  - Rol: **hardcoded** liste (`MANAGER`, `PURCHASE_MANAGER`, `SYSTEM_ADMIN`, `FINANCE_MANAGER`, `DEPARTMENT_HEAD`, `GENERAL_MANAGER`) — API roller değil
  - stepName alanı UI’da **yok** (validation da stepName istemiyor; sadece roleName)
  - Yeni adım: `approverType: 'USER'` (Create’te `ROLE`)
- **Tablo kolonları:** Yok
- **Butonlar / aksiyonlar:** İptal (dirty); Kaydet; Adım Ekle/move/remove; load fail → listeye dön
- **Filtreler / arama:** Yok
- **Modal/popup:** confirm iptal
- **Kullanılan API’ler:**
  - `getWorkflowById` → `GET /api/approval-workflows/{id}`
  - `updateWorkflow` → `PUT /api/approval-workflows/{id}`
- **Navigasyon:** `/workflows`
- **Edge cases:** Create vs Edit rol kaynağı tutarsız; stepName edit UI eksik; loading / error states

---

## UserGroupWhiteboard

- **Route:** `/user-groups/whiteboard`
- **Security:** PrivateRoute. Menü SYSTEM_MANAGE. Sayfa içi check yok.
- **Sayfa tipi:** detail / whiteboard (özel UI)
- **Amaç / özellikler:** React Flow ile grup kutuları, bağlantılar, üyeler, pozisyon kaydı.
- **Gösterilen alanlar / form fieldları:**
  | Alan | Required | Not |
  |------|----------|-----|
  | newGroupName | hayır | boş → “Yeni Grup” |
  | editGroupName | — | seçili grup |
  | editGroupDescription | isteğe bağlı | |
  | user checkboxes | — | üye seti |
  | userSearchQuery | — | ad/soyad/email/departman |
- Node data: label, memberCount, description
- **Tablo kolonları:** Yok; sağ panel kullanıcı listesi (20/sayfa)
- **Butonlar / aksiyonlar:**
  - Yeni Grup; Pozisyonları kaydet; Yenile
  - Güncelle / Kopyala / Sil (grup)
  - Üyeleri kaydet; Kullanıcı listesini yenile
  - Pagination Önceki/Sonraki
  - Canvas: drag pozisyon (auto PATCH), connect link, Delete/Backspace sil
  - Hata: Tekrar dene
- **Filtreler / arama:** Kullanıcı arama + pagination (20)
- **Modal/popup:** `window.confirm` silme; `alert` hata (bağlantı/grup)
- **Kullanılan API’ler:**
  - `getWhiteboardData` → `GET /api/user-groups/whiteboard`
  - `getGroupById` → `GET /api/user-groups/{id}`
  - `createGroup` → `POST /api/user-groups`
  - `updateGroup` → `PUT /api/user-groups/{id}`
  - `updateGroupPosition` → `PATCH /api/user-groups/{id}/position?positionX&positionY`
  - `updateGroupPositions` → `PUT /api/user-groups/positions`
  - `deleteGroup` → `DELETE /api/user-groups/{id}`
  - `createLink` → `POST /api/user-groups/links`
  - `deleteLink` → `DELETE /api/user-groups/links/{id}`
  - `setGroupMembers` → `PUT /api/user-groups/members` `{ userGroupId, userIds }`
  - `userService.getAllUsers` → `GET /api/users`
- **Navigasyon:** Sayfa içi; Navigation Sistem menüsü
- **Edge cases / conditional UI:**
  - Seçim yok → sağda boş durum
  - Kopya: üyeler kopyalanmaz (`memberCount: 0`, selectedUserIds=[])
  - Aynı/çift bağlantı alert
  - Masaüstü odaklı (React Flow + 320px aside); mobil için zor
  - Success/error toast benzeri kısa mesajlar
  - usersError / boş kullanıcı yönlendirmesi

---

## Mobil port için kısa notlar

1. Sistem sayfalarında route’ta CapabilityRoute yok; menü SYSTEM_MANAGE ile gizleniyor — mobilde aynı capability’yi uygulayın.
2. Workflow Create/Edit rol kaynakları farklı; portta birleştirin.
3. Login `/register` ölü link.
4. UserGroupWhiteboard native graph UI veya basitleştirilmiş liste/detay ister.
5. Dashboard “görüldü” localStorage — cihazlar arası senkron yok.
6. PermissionList’te edit yok; isActive UI’da yok.